import styled from "styled-components";

export const ContentApisContainer = styled.div`
  margin-left: calc(var(--categories-width) + var(--size20px));
  margin-top: var(--header-height);
  width: 65%;
`;
